import { DealListView } from 'src/sections/deals/view/deals-list-view';

export const metadata = {
  title: 'Deals | Mumbai Angels',
};

export default function DealListPage() {
  return <DealListView />;
}
