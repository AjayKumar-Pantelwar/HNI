import { OverviewAppView } from 'src/sections/overview/app/view';

export const metadata = {
  title: 'Dashboard | Mid Market',
};

export default function OverviewAppPage() {
  return <OverviewAppView />;
}
