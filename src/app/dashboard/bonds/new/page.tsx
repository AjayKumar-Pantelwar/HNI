import { BondsCreateView } from 'src/sections/bonds/view';

export const metadata = {
  title: 'Create | Bond | Mid Market',
};

export default function RoleCreatePage() {
  return <BondsCreateView />;
}
