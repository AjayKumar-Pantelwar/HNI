import { GetCarouselResponse } from 'src/types/carousel.types';

interface Props {
  data: GetCarouselResponse;
}

const CarouselGridView = ({ data }: Props) => {
  return <div>CarouselGridView</div>;
};

export default CarouselGridView;
